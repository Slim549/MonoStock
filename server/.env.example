# Server Configuration
PORT=3000
NODE_ENV=production

# Data storage path (defaults to ./data/dashboard.json)
DATA_DIR=./data

# CORS origin (set to your frontend domain in production)
CORS_ORIGIN=*

# Backup settings
MAX_BACKUPS=20
BACKUP_INTERVAL_MS=1800000

# Authentication (placeholder — wire up your auth provider)
# AUTH_SECRET=your-secret-here
# AUTH_ENABLED=false
